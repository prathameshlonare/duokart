# Copy to .env (never commit .env). All AWS via Console — no keys here.
AWS_MODE=local
AWS_REGION=us-east-2
DB_HOST=localhost
DB_NAME=duokart
DB_USER=duokart
DB_PASSWORD=change-me-local-only
S3_PHOTOS_BUCKET=duokart-photos-local
S3_BILLS_BUCKET=duokart-bills-local
SQS_QUEUE_URL=local
SNS_BUYER_TOPIC=local
SNS_OWNER_TOPIC=local
